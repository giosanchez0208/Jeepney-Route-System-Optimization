# Direct dependencies only. Pins are the versions the thesis runs were executed
# against (Python 3.11+). Everything else in the environment is transitive.
#
#     pip install -r requirements.txt
#
# pyrosm is the heavy one: it compiles against a C extension and is only needed
# when a CityGraph is built from a .pbf extract. Analysis and figure scripts that
# read the cached pickles in data/cache/ do not import it.

# --- numerics / data ---
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1

# --- geospatial + graph ---
networkx==3.6.1
osmnx==2.1.0
pyrosm==0.6.2

# --- rendering ---
matplotlib==3.10.9
pillow==12.2.0

# --- config, io, cli ---
PyYAML==6.0.3
python-dotenv==1.2.2
requests==2.33.1
rich==15.0.0
tqdm==4.67.3

# --- notebooks (only needed for notebooks/) ---
jupyterlab==4.5.7
ipywidgets==8.1.8
